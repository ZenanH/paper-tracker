# 论文追踪网页：任务与实施约定

确认日期：2026-09-19。本文记录用户已确认的完整需求，作为后续实现、部署和维护的依据。

## 当前阶段

- 总体设计已经确认，网站和数据管道已开始实现；后续继续按本文完成部署和运行验证。
- 当前已完成静态前端、期刊分区快照生成、Crossref 采集、三个月数据回填、测试及本地静态数据验证。
- 站点、采集、翻译、指标检查和期刊管理在本机 Docker 自托管运行；日常任务不依赖 CI，程序镜像由 GitHub CI 构建发布。
- 程序镜像由 GitHub CI 构建发布；论文、翻译、归档、收藏和运行时期刊配置保存在宿主持久目录，不进入镜像或代码仓库。
- 中文翻译在本机执行；模型调用仅通过配置的 OpenAI 兼容接口，凭据只存本机受保护文件。
- 用户不要求独立的翻译质量测试，不安排前置的百篇翻译评测；仍须验证程序正常运行、输出完整及资源使用情况。
- 不把设计确定或数据源存在表述为整个系统已经运行验证通过。

## 目标与边界

- 在本机以 Docker 承载静态站点、归档 API 和期刊管理/调度服务，不依赖外部托管服务或独立数据库。
- 跟踪 journal.json 的 categories 下列出的期刊，目前共 32 本；removed 中的期刊不参与采集。
- 保留用户的“顶刊、一区、二区”分组。“顶刊”是个人分组，不替代中科院分区或官方 Top 字段。
- 首页默认展示北京时间昨日新上线的论文，按期刊分组，仅展示中英文标题及论文直达链接。
- 不增加摘要、作者长列表等未要求的主要展示内容；必要的日期、来源、状态信息可以保留。
- 支持滚动三个自然月的历史查询、日期选择和历史补录入口。
- 不自动增加收费服务，也不要求用户额外注册翻译服务或论文数据库账号。

## 用户信息与凭据

- Crossref 联系邮箱通过本机环境变量 `CROSSREF_MAILTO` 提供。
- 邮箱仅作为采集请求的联系信息，不在网页界面主动展示，也不写入源码；未配置时不附带邮箱参数。
- 不要求用户提供密码或账号；凭据一律通过一次性掩码通道交付，不进入对话、日志或前端产物。
- 实施时先检查本地目录结构与运行状态，不假设某种托管环境已经初始化。
- 站点运行不依赖外部仓库或云端运行器；如需镜像分发等可选功能，先说明影响再决定。
- 不覆盖无关内容，不撤销用户已有修改；路径或端口冲突时先说明。
- 敏感凭据只放本机受保护文件（权限 0600），不进入源码、日志、前端产物或数据目录。
- `data/` 与 `archive/` 是运行数据目录，必须独立于 Docker 镜像和 Git；容器升级不得覆盖，CI 也不得把其中内容打包进镜像。归档与收藏共同存放于 `archive/archive.json`。

## 时间与论文归档规则

- 业务时区统一为 Asia/Shanghai。
- 每天北京时间 01:00 启动采集更新。采用 UTC 定时表达式时为 `0 17 * * *`。
- 定时任务允许调度延迟，不承诺 01:00:00 完成发布；页面展示实际最后成功更新时间及对应业务日期。
- 支持手动运行和失败重试；运行须可重入、幂等，避免并发写入覆盖。
- 按论文首次在线发表日期归档，而不是按本站首次抓取日期或正式卷期日期归档。
- 保存原始日期、日期字段来源、日期精度、首次发现时间及最后更新时间。
- 有完整且可解释时区的时间戳时转换为北京时间；只有年月日时使用来源日期并记录仅日期精度，不伪造时区或午夜时刻。
- 只有年月、只有年份或无法确认发表日期时进入“日期待核实”，不能用抓取日期伪装发表日期。
- 同一 DOI 从在线优先发表转入正式卷期，不重复作为新论文发布。
- 默认收录研究论文及综述；更正、撤稿通知、社论等单独分类，不混入研究论文日报。
- 主页默认日期始终是北京时间昨日；若更新失败，保留真实状态，不静默将旧日报冒充昨日内容。

## 采集与去重

- Crossref REST API 为统一基础数据源，按 ISSN/eISSN 获取论文，使用上述邮箱标识联系信息并遵守限流。
- 为所有期刊建立稳定 ID、ISSN/eISSN、名称别名及官方主页映射，不仅靠期刊名称模糊匹配。
- 出版社官方 RSS/API 为补充；确需页面解析时逐刊适配，处理访问失败，不绕过访问控制。
- 使用元数据更新/收录时间维护增量同步进度，发表日期仅用于归档，不能只查询“昨日发表”而忽略迟到数据。
- 每日回查近期记录，每周核对整个保留窗口；合理分页、限流、退避、超时和重试。
- 每周全窗口核对由 `journal-manager` 持久化记录在 `data/maintenance-status.json`；日更与翻译优先完成，核对到期后再执行三个月 backfill，成功校验后才更新核对日期。
- DOI 规范化后作为首选去重键；无 DOI 时使用有依据的来源标识或规范化链接，不仅凭相似标题合并。
- 已有 DOI 的发布日期被来源修正时，必须从旧日期文件原子移除并写入新日期；日期待核实记录取得完整日期后必须从 `date_pending` 删除，不能在两个位置残留。
- 保存每本期刊的采集状态和最后成功时间，区分“获取成功但无新论文”与“获取失败”。
- 单一期刊或单一来源失败不清空已有数据，不阻塞其他期刊的有效更新。
- 发布数据使用原子更新或等效机制，不让网页读到半写入文件。
- 实施中核对 32 本期刊的实际数据覆盖情况；缺失或不支持的数据源必须明确报告，不能保证未经验证的完整性。

## 历史与补录

- 论文按日期保存为静态结构化数据，网页通过日期索引读取，不需后端查询服务。
- 保留滚动三个自然月，采用日历月运算并明确窗口边界，不将“三个月”默认为固定 90 天。
- 首次运行尝试回填窗口内论文；不能声称还原了本站启用前真实的首次发现时间。
- 初次回填和翻译分批执行、保存进度；每日昨日论文处理优先，回填不阻塞日常更新。
- “历史补录”入口包含两类：
  - 迟到补录：发表日期明确，回填原日期，并显示发现时间及对应归档日期。
  - 日期待核实：发表日期不完整，独立展示；取得可用日期后自动归档并更新状态。
- 只有年月日但没有时分秒不属于日期待核实。
- 日期待核实记录按首次发现日期保留三个自然月；窗口外论文不因迟到而扩展保留窗口。
- 初次批量回填与日常迟到补录应区分，避免首次回填淹没补录提示。
- 提供日期日历、前一天、后一天、返回昨日；清楚区分无论文、尚未更新、加载失败和窗口外日期。
- 三个月是网页查询窗口，不承诺清除 Git 历史中的全部旧数据；不为清理窗口擅自重写仓库历史。

## 中科院分区：固定 2025 升级版

- 按用户指定，使用官方平台停服前的中科院分区整理数据，不要求学校账号作为实施前提。
- 使用 `中科院分区表及JCR原始数据文件/FQBJCR2025-UTF8.csv`，不能混用 JCR 分区或 XR 新锐分区。
- 源文件已随仓库归档（`scripts/snapshot/FQBJCR2025-UTF8.csv`），版本由下方 blob 校验值锁定。
- 本轮核查的 CSV Git blob SHA：`5918c4ee712878e2b6bc2e5d50f7a87b3c67a719`；它是 Git 对象标识，不是普通文件 SHA-256。
- 首次实施时验证内容并生成文件校验值，提取 32 本期刊为本项目快照；此后运行完全离线，不再联网取分区数据。
- 保存大类及分区、小类及各自分区、Top、ISSN/eISSN、数据版本、源文件标识、提取日期及校验值。
- 页面主要展示大类分区；小类信息可放在展开详情中，不把两者混淆。
- 来源说明应为“2025 升级版（官方平台停服前快照）”，不宣称我们直接获得官方认证。
- 不把 2025 固定快照自动升级成其他年份或新锐分区。
- 已完成的本地比对：32 本均在表内找到；现有 29 本已填大类分区与该表一致。
- 三本个人“顶刊”在该表中均为一区、Top：Communications Earth & Environment 为地球科学；Nature Communications 与 Science Advances 为综合性期刊。
- Future Generation Computer Systems 在表中的名称为 `Future Generation Computer Systems-The International Journal of eScience`，ISSN/eISSN 为 `0167-739X/1872-7115`。
- 不将 journal.json 原有“官方平台停服前快照”说明直接沿用为本次来源，应按实际提取来源修正。
- 用户已指定个人网页用途及此数据源，不将版权讨论作为本次技术实施的前置阻塞；保留来源归属，不扩大为未经核实的授权声明。

## 影响因子

- 每三个日历月检查一次出版社官方期刊指标页面；初次实施也核验现有值。
- 影响因子为年度指标，季度任务是检查新版和修订，不是每季度制造一个新指标年份。
- 保存数值、指标年份、报告版本（可确认时）、来源链接、抓取/检查日期和检查状态。
- 区分指标年份和 JCR 报告发布年份，核验 journal.json 中现有 if_year 和 if_source，不照搬尚未核实的日期声明。
- 只把明确标注的 Journal Impact Factor/JIF 作为影响因子，不使用 CiteScore、SJR、五年影响因子等替代。
- 抓取失败、年份不明或解析存在歧义时保留旧值并标记待核实，不写零、不无提示覆盖、不把旧值标成新年份。
- 逐刊维护解析与来源映射；出版社页面不能稳定自动解析时报告该刊限制，允许保留带年份的旧值。
- 不要求购买 Clarivate API，也不默认配置收费备用服务。
- `sync_cas.py` 重建分区主数据时必须按期刊 ID 保留已有 `impact_factor` 和全局 `metrics_checked_at`；新增单刊的定向指标检查不得推进全局季度检查时间。

## 翻译

- 在本机调用用户配置的 OpenAI 兼容 `POST /v1/chat/completions` 接口；模型调用只在每日任务中按需进行。
- 必填配置为环境变量 `LLM_API_KEY`、`LLM_BASE_URL` 和 `LLM_MODEL`（存放于项目根目录 `.env`，权限 0600，Git 忽略）。基础地址通常填写到 `/v1`，脚本追加 `/chat/completions`；也兼容直接填写完整 endpoint。
- API 请求使用单一 `translate_titles` function tool，函数参数使用 `strict: true`、`additionalProperties: false`，要求返回 `{translations: [{id, title_zh}]}`。
- 不能仅相信模型或 `strict`：程序必须检查 JSON 可解析、ID 集合完全一致、没有重复/多余/缺失 ID、中文标题非空，并按输入 ID 顺序重排。
- 如果供应商不接受 `tool_choice`，首次请求失败时允许重试一次并去掉强制选择；仍须通过同样的结构和语义校验。普通自然语言或不完整结果不得写入数据。
- 翻译缓存键必须包含 `openai-compatible:<model>` 引擎标识；切换模型时旧译文标记为待重译，不能把旧模型译文冒充新模型结果。
- 日更任务只处理新论文和未完成译文；历史重翻由手动 `--retranslate-existing` 分批执行，不占用每日采集任务的主要运行时间。
- API Key 只能存在本机受保护文件，不得出现在源码、脚本参数、网页、数据目录或日志中；请求错误日志必须脱敏。
- 翻译失败或未完成时显示英文并标记待翻译，网络/API 重试后仍失败也不阻塞整日报；后续运行可继续处理。
- 保留英文原题，展示中文译题；保护公式、符号、缩写和专名，清洗出版社 HTML/实体并安全渲染，不能把标题当可执行 HTML。
- 首次三个月回填分批执行、可恢复；用户明确不要求独立翻译质量评测，但必须做结构、完整性和资源用量核验。
- 日期文件与补录中引用同一标题缓存键时，每轮只调用模型一次，并把结果回写全部关联记录；`--limit` 按唯一标题数计算。

## 网页设计

- 现代、简洁、安静的阅读工具，不做营销落地页、巨幅 hero 或装饰性卡片堆叠。
- 首屏直接呈现日期、更新状态和论文列表，按期刊分组。
- 期刊组标题包含期刊名称、论文数量、2025 中科院大类分区、Top（适用时）及带年份的影响因子。
- 每篇文章展示中文标题和英文原题，两者都可直达论文；优先使用官方文章链接或 DOI 链接。
- 桌面提供紧凑期刊导航；手机使用单列和可折叠筛选，交互目标大小适合触摸。
- 支持按期刊/分组筛选以及历史日期查询，保持当前日期和筛选上下文。
- 补录入口清楚但不喧宾夺主，区分补录与待核实数量。
- 设置按钮允许在当前浏览器隐藏或恢复已有期刊；页面不直接修改 `journal.json`，也不把本地显示偏好误当成服务端采集配置。
- 设置中的期刊管理允许输入英文全名新增期刊、删除期刊和立即运行日更；这些操作修改数据目录内的持久配置，不修改镜像内默认种子。
- 期刊筛选的“全部选择”按钮必须根据当前状态在“全部选择”和“全部取消”之间切换；空选择也要通过 URL 保持。
- 页面顶部展示最近一次采集的期刊成功/失败统计与更新时间，数据直接读取本地 manifest，不依赖外部接口。
- 设置中提供浅色、深色和跟随系统主题，并避免首屏主题闪烁。
- 处理长标题、长刊名、公式、窄屏、空状态、加载状态和错误状态；不能横向溢出、遮挡或布局跳动。
- 使用可访问语义、键盘操作、可见焦点和合理对比度；日历控件支持移动端操作。
- 不为了装饰引入无关图片、图表或额外论文内容，优先阅读效率和加载速度。

## 页面结构

- 四个大标签：**每日论文**、**历史及补录**、**已归档**、**收藏**。
- 每日论文固定展示昨日（`manifest.default_date`）论文，日期不可更改。
- 历史及补录下分两个小标签：**历史**（按日浏览）、**补录**（迟到补录 + 日期待核实合并展示）。
- 日期选择器只在“历史”子标签生效并显示；“补录”是跨日期的待处理集合，不展示无效的日期控件。
- 已归档按论文日期展示，默认昨日，用户可在滚动三个月窗口内切换日期。
- 收藏不设日期筛选，集中展示用户主动收藏的全部论文，并按期刊分组。
- 标签角标已提供数量时，内容区不再重复添加同义标题或数量摘要。

## 已归档（已读标记）

- 「每日论文」每篇论文与每本期刊标题旁提供勾选框；勾选即代表用户已读。
- 单篇勾选归档该篇；期刊级「全部已读」勾选归档该刊当前列表全部论文。
- 不设「全部取消」逻辑；归档后论文从每日列表移出，计数单独标注。
- 「已归档」标签列出全部已读论文，支持单篇与期刊整组勾选后「恢复所选」，便于复查。
- 「清空归档」将当前可见归档转为已确认看完的隐藏归档；论文仍保持已读，不重新出现在每日或历史列表。
- 设置中提供「恢复归档」，将清空后隐藏的归档重新显示；这与将论文恢复为未读的「恢复所选」含义不同。
- 归档状态持久化在 `archive/archive.json`，由容器内 `archive-api` 服务读写（经 nginx `/api/` 反代，不对外暴露端口）。
- 归档文件缺失时可初始化为空；文件已存在但 JSON 损坏或结构错误时 API 必须返回错误并拒绝写入，不能静默当作空状态覆盖原文件。
- 归档跨设备共享；清除浏览器缓存不影响。归档文件属主须为站点使用者，便于宿主维护。
- 归档只记录阅读状态，不修改采集数据、不改动 `data/` 目录，也不影响每日任务。
- Docker 使用 `.env` 中的 `PAPER_TRACKER_DATA_DIR` 和 `PAPER_TRACKER_ARCHIVE_DIR` 指定宿主持久目录；论文数据、补录和翻译缓存随滚动三个自然月窗口清理。

## 收藏

- 「每日论文」「历史及补录」（含历史与补录）和「已归档」中的每篇论文都提供星标收藏按钮。
- 收藏与已读归档是两套独立状态：收藏论文后再归档、清空归档、恢复归档或恢复为未读，均不得改变收藏状态。
- 「收藏」标签列出全部收藏论文，按期刊分组；再次点击已点亮的星标即取消收藏，并立即从收藏页移除。
- 收藏状态通过同一个 `archive-api` 持久化在 `archive/archive.json` 的 `favorites` 字段中，跨浏览器和跨设备共享，容器或镜像升级不得覆盖。
- 收藏条目保存论文 ID、期刊 ID、论文日期、中英文标题、链接、DOI、翻译状态和收藏时间，以便原日期文件滚出窗口后仍可查看。
- 三个月限制适用于论文查询数据与已读归档窗口，不自动删除用户主动收藏的论文；收藏只在用户取消收藏或删除对应期刊时移除。
- 删除期刊时必须同时清理该刊的可见归档、已清空归档和收藏记录，不影响其他期刊。
- 收藏按钮使用固定尺寸的星标图标，提供键盘焦点、`aria-label` 和 `aria-pressed`，切换状态不得导致标题布局跳动。

## 实施与验收

- 镜像内 `journal.json` 作为默认期刊种子；首次运行复制为持久化的 `data/journal-config.json`，之后用户增删不得被镜像升级覆盖。
- 新增期刊必须先在固定 2025 分区快照匹配刊名和 ISSN，生成分区信息后回填三个月论文、尝试核验 JIF 并翻译；无法核验的指标明确标记待核实，不推测 JCR quartile 或 JIF。
- 删除期刊时清理其日期论文、补录、采集状态、归档状态、收藏状态及无引用翻译缓存，不影响其他期刊。
- 常驻管理容器按 `Asia/Shanghai` 每天 01:00 运行日更；UI 手动任务与定时任务互斥，避免并发写入。
- 管理容器启动时恢复 `data/task-status.json`；上次运行中的任务标记为中断。若 manifest 落后于北京时间昨日，启动后立即补跑一次，不等待下一天 01:00。

- 先检查现有项目内容和工具链，选择简单、可维护、适合静态部署的实现，不为本任务引入不必要的服务。
- 将采集、元数据、翻译、归档和前端展示合理分离，各任务可独立在本机运行。
- 完成论文日更、季度指标检查、历史回填/恢复及运行流程；说明各任务如何手动触发。
- 关键测试覆盖北京时间日期边界、月份边界、DOI 去重、迟到补录、待核实转归档、窗口清理、收藏与归档状态独立性、旧状态文件兼容和翻译缓存。
- 使用固定样例测试指标解析，防止错误年份或其他指标被误写成 JIF。
- 检查网络失败、部分来源失败、重复执行、任务中断及并发运行时的数据完整性。
- 在桌面和手机尺寸检查页面及交互，核验真实标题、链接、日历、筛选和补录入口。
- 查看定时任务与容器的实际执行结果，报告未验证或失败的来源，不能仅凭配置存在宣布自动更新成功。
- 给出使用与维护说明：数据来源、日期规则、固定分区版本、指标年份、失败状态、任务恢复和三个月保留规则。
- 开发完成后提供实际可访问的站点地址；无法访问时说明具体阻碍，不虚构已上线结果。

## 文档边界

- `README.md` 保持简洁，只说明项目主要功能、Docker Compose 首次部署、访问、状态查看和升级命令。
- 数据口径、采集策略、翻译协议、页面行为、持久化、镜像结构、手动任务、CI、测试和维护约定统一记录在本文件。
- README、示例环境文件和 Compose 配置不得写入真实机器 IP、API Key、邮箱或其他用户私密信息；地址只使用环境变量、动态命令或占位符。

## Docker 部署与维护

- 需要 Docker、Docker Compose 和 Tailscale；站点同时绑定 `.env` 中的 `PAPER_TRACKER_TAILSCALE_IP` 与 `127.0.0.1`。
- 首次部署复制 `.env.example` 为 `.env` 并设为 `0600`；可用 `tailscale ip -4` 动态取得地址，不得把结果提交到 Git。
- `.env` 主要配置：
  - `PAPER_TRACKER_TAILSCALE_IP`、`PAPER_TRACKER_PORT`：站点监听地址和端口。
  - `PAPER_TRACKER_UID`、`PAPER_TRACKER_GID`：写入持久目录的宿主用户身份。
  - `PAPER_TRACKER_DATA_DIR`：论文、补录、翻译缓存、指标和运行时期刊配置，默认 `./data`。
  - `PAPER_TRACKER_ARCHIVE_DIR`：归档与收藏状态，默认 `./archive`。
  - `CROSSREF_MAILTO`：Crossref 联系邮箱，可留空。
  - `LLM_BASE_URL`、`LLM_API_KEY`、`LLM_MODEL`：OpenAI 兼容翻译接口配置。
- 首次启动前创建 `data/days` 和 `archive`，并确保其属主与 `PAPER_TRACKER_UID:PAPER_TRACKER_GID` 一致。
- 不使用 Docker 时，可在项目根目录运行 `python3 -m http.server 8000` 进行静态页面预览；必须通过 HTTP 访问，不能依赖直接打开 `index.html` 读取 JSON。
- Compose 常驻三个服务：
  - `paper-tracker`：nginx 静态站点镜像 `ghcr.io/zenanh/paper-tracker:latest`。
  - `archive-api`：归档与收藏 API 镜像 `ghcr.io/zenanh/paper-tracker-archive:latest`，只在容器网络监听。
  - `journal-manager`：期刊管理和北京时间 01:00 调度服务，使用 `ghcr.io/zenanh/paper-tracker-tasks:latest`。
- `tasks` 为按需 profile，可通过 `docker compose --profile tasks run --rm tasks <命令>` 手动执行：
  - `python3 scripts/sync_cas.py`：从固定快照重新生成期刊分区主数据。
  - `python3 scripts/collect.py --mode daily`：采集日更及迟到补录。
  - `python3 scripts/collect.py --mode backfill`：回填滚动三个月。
  - `python3 scripts/translate.py --limit 300`：翻译待处理标题。
  - `python3 scripts/translate.py --retranslate-existing --limit 300`：按当前模型分批重翻保留窗口。
  - `python3 scripts/update_metrics.py`：检查影响因子。
  - `python3 scripts/validate_data.py`：校验生成数据。
- 常规部署命令为 `docker compose pull` 和 `docker compose up -d`；升级使用 `git pull --ff-only` 后执行 `docker compose pull`、`docker compose up -d --remove-orphans`。
- 容器替换不得修改 `data/`、`archive/` 或 `.env`；迁移时须备份这三个位置。
- 站点 nginx 固定使用单 worker，关闭 access log，避免低流量个人部署浪费进程并记录访问地址；保留错误日志和健康检查。

## GitHub CI 与镜像

- `.github/workflows/docker-ci.yml` 在 push 和 Pull Request 时使用 `ubuntu-latest`、`windows-latest`、`macos-latest` 执行 Python 编译检查和单元测试。
- 三平台测试通过后，在 Linux 构建站点、归档 API 和任务三个 `linux/amd64`、`linux/arm64` 多架构镜像。
- 推送到 `main` 时发布 `latest` 和 `sha-*` 标签到 GHCR；Pull Request 只验证，不发布。
- CI 使用仓库 `GITHUB_TOKEN` 发布镜像，不读取 LLM 或 Crossref 凭据；运行数据不进入 Git，也不会因日常采集触发镜像构建。
